services:
  postgres:
    image: postgres:16-alpine
    container_name: cropsakha-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cropsakha
      POSTGRES_USER: cropsakha
      POSTGRES_PASSWORD: cropsakha_dev_2026
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cropsakha"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
